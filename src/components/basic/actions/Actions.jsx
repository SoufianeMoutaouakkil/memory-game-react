import React from 'react';

const Actions = ({shuffle}) => {
    return (
        <>
            <select name="" id="">

            </select>
            <button className="btn-basic" onClick={shuffle}>New Game</button>
        </>
    );
}

export default Actions;
